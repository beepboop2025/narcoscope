# ➡️ Moved to the sinoscope-platform monorepo

Active development of this project now lives in the **sinoscope-platform** monorepo:

> https://github.com/beepboop2025/sinoscope-platform → **`apps/drug-observatory`**

This standalone repository is preserved for history and is **no longer the source of
truth**. Make changes in the monorepo (`apps/drug-observatory`), not here — editing
both places causes divergence that has to be reconciled by hand. Full commit history
was migrated into the monorepo via `git subtree`.
